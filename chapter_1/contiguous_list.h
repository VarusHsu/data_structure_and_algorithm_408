# ifndef CONTIGUOUS_H_
# define CONTIGUOUS_H_
# define INITIAL_CAPACITY (0)

template<typename T>

class Contiguous{
    private: 
    T* header;
    int size;
    int capacity;
    public:
    Contiguous();
};
# endif